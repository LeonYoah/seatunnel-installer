name: Create Release

on:
  push:
    tags:
      - 'v*' # 当推送v开头的tag时触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Create Release Package
        run: |
          mkdir seatunnel-installer-${GITHUB_REF#refs/tags/}
          cp install_seatunnel.sh seatunnel-installer-${GITHUB_REF#refs/tags/}
          cp uninstall_seatunnel.sh seatunnel-installer-${GITHUB_REF#refs/tags/}
          cp config.properties seatunnel-installer-${GITHUB_REF#refs/tags/}
          cd seatunnel-installer-${GITHUB_REF#refs/tags/}
          tar -czf seatunnel-installer-${GITHUB_REF#refs/tags/}.tar.gz *
          
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            seatunnel-installer-${GITHUB_REF#refs/tags/}/seatunnel-installer-${GITHUB_REF#refs/tags/}.tar.gz
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 